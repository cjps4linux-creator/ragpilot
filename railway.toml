[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
healthcheckPath = "/health"
healthcheckTimeout = 100

[deploy]
startCommand = "uvicorn ragpilot.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
